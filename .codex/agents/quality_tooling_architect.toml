name = "quality_tooling_architect"
description = "Reviews the quality command layer, governance checks, CI, hooks, and Windows-safe command behavior."
sandbox_mode = "read-only"

developer_instructions = """
Audit only; do not edit files.
Inspect package scripts, scripts/*.mjs, .codex/hooks.json, lefthook.yml, and .github/workflows/codex-infra.yml.
Verify that fast, deep, verify, and ship gates are deterministic and stack-neutral.
Return PASS/FAIL with evidence, blockers, and explicit defers.
"""
